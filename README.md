# Ingatlan.com próbamunka

## Telepítés

```
    npm install
    .env változó létrehozása .env.example alapján
    npm run start
```

### .env
A .env fájlban kell megadni az url-t, ahonnan a példa json adatokat kérjük le a frontendhez.


## Minimum verziók

- Node 16
- npm 8